CXX_STD = CXX11

PKG_CXXFLAGS = -O3 -fopenmp
PKG_FFLAGS   = -O3 -fopenmp

PKG_LIBS = -fopenmp $(FLIBS)

# Maybe consider use this later (might need to be changed if linux/mac/window)
# "I" location of files
# PKG_CPPFLAGS = -I../inst/include

# "L" location of .so files and -lmtgrs links libmtgrs.so
# PKG_LIBS = -L../inst/libs -lmtgrs \
#           $(SHLIB_OPENMP_CXXFLAGS) \
#           $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
