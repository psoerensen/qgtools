% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernel_constructor.R
\name{makeGRMlist}
\alias{makeGRMlist}
\title{Create a GRMlist object}
\usage{
makeGRMlist(fnGRM = NULL, format = "BINARY", grm_type = "G")

makeGRMlist(fnGRM = NULL, format = "BINARY", grm_type = "G")
}
\arguments{
\item{fnGRM}{Character string. File name of the GRM (or its inverse).}

\item{format}{Character string. Format of the GRM file
(\code{"TEXT"} or \code{"BINARY"}).}

\item{grm_type}{Character string specifying the GRM representation.
Typical values include \code{"G"}, \code{"Ginv"}, or \code{"scaled_G"}.}
}
\value{
An object of class \code{"GRMlist"}.

An object of class \code{"GRMlist"}.
}
\description{
Create a genomic relationship matrix (GRM) specification object used to
define additive genetic relationships based on marker data.

Create a genomic relationship matrix (GRM) specification object used to
define additive genetic relationships based on marker data.
}
\details{
The \code{GRMlist} object contains no phenotype or model-specific information.
Linking the GRM to analysis data is handled at a higher (model) level.

The \code{GRMlist} object contains no phenotype or model-specific information.
Linking the GRM to analysis data is handled at a higher (model) level.
}
